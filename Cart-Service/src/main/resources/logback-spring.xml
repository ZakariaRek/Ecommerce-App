<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Default Spring Boot Console Appender -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- Your Custom Console Appender -->
    <appender name="CUSTOM_CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss} - %msg%n</pattern>
        </encoder>
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>DEBUG</level>
        </filter>
    </appender>

    <!-- Configure specific loggers -->
    <logger name="org.springframework.kafka" level="DEBUG" additivity="false">
        <appender-ref ref="CUSTOM_CONSOLE"/>
    </logger>

    <logger name="org.apache.kafka" level="WARN" additivity="false">
        <appender-ref ref="CUSTOM_CONSOLE"/>
    </logger>

    <logger name="com.Ecommerce.Cart.Service.Lisiteners" level="DEBUG" additivity="false">
        <appender-ref ref="CUSTOM_CONSOLE"/>
    </logger>

    <logger name="com.Ecommerce.Cart.Service.Config.KafkaProducerConfig" level="DEBUG" additivity="false">
        <appender-ref ref="CUSTOM_CONSOLE"/>
    </logger>

    <!-- Root logger with default console -->
    <root level="INFO">
        <appender-ref ref="STDOUT"/>
    </root>

</configuration>